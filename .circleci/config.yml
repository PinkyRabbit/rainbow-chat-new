version: 2.1

jobs:
  build:
    docker:
      - image: circleci/node:10.17.0
    working_directory: ~/repo
    steps:
      - checkout
      - run:
          name: deploy
          command: ssh -o StrictHostKeyChecking=no $VPS_USER@$VPS_IP "/var/www/$PROJECT_FOLDER/deploy.sh"
